<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adicionar Livro - Biblioteca Ativa</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/adicionarLivro.css">
</head>
<body>
    <header class="header">
        <div class="header-container">
            <a href="/home" class="logo">
                <i class="fas fa-book-open"></i> Biblioteca Ativa
            </a>
            
            <button class="mobile-menu-btn">
                <i class="fas fa-bars"></i>
            </button>
            
            <ul class="nav-menu">
                <li><a href="/home" class="nav-link">Home</a></li>
                <li><a href="/catalogo" class="nav-link">Catálogo</a></li>
                <li><a href="/catalogo/acervo" class="nav-link">Acervo</a></li>
                <li><a href="/catalogo/adicionar" class="nav-link active">Adicionar Livro</a></li>
            </ul>
            
            <a class="user-menu" href="/perfil">
                <div class="user-avatar">
                    <%= user.nome.charAt(0).toUpperCase() %>
                </div>
                <span class="user-name"><%= user.nome %></span>
            </a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="main">
        <div class="container">
            <!-- Page Header -->
            <div class="page-header">
                <h1 class="page-title">Adicionar Novo Livro</h1>
                <p class="page-subtitle">Adicione um novo livro ao acervo da biblioteca</p>
            </div>

            <!-- Alertas -->
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-error">
                    <i class="fas fa-exclamation-triangle alert-icon"></i>
                    <div class="alert-content">
                        <strong>Erro:</strong> <%= error %>
                    </div>
                </div>
            <% } %>

            <!-- Form Container -->
            <div class="form-container card">
                <form action="/catalogo/adicionar" method="POST" class="book-form">
                    <!-- Título -->
                    <div class="form-group">
                        <label for="titulo" class="form-label">
                            <i class="fas fa-bookmark"></i> Título *
                        </label>
                        <input type="text" class="form-input" id="titulo" name="titulo" 
                               value="<%= typeof titulo !== 'undefined' ? titulo : '' %>" 
                               placeholder="Digite o título do livro" required>
                        <div class="form-text">Ex: Dom Casmurro, O Senhor dos Anéis, etc.</div>
                    </div>

                    <!-- Autor -->
                    <div class="form-group">
                        <label for="autor" class="form-label">
                            <i class="fas fa-user"></i> Autor *
                        </label>
                        <input type="text" class="form-input" id="autor" name="autor" 
                               value="<%= typeof autor !== 'undefined' ? autor : '' %>" 
                               placeholder="Nome do autor" required>
                        <div class="form-text">Ex: Machado de Assis, J.R.R. Tolkien, etc.</div>
                    </div>

                    <!-- Capa -->
                    <div class="form-group">
                        <label class="form-label">
                            <i class="fas fa-image"></i> Capa do Livro
                        </label>
                        <div class="capa-search-container">
                            <button type="button" class="btn btn-outline" id="btnBuscarCapa">
                                <i class="fas fa-search"></i> Buscar Capa Online
                            </button>
                            <div class="form-text">
                                Use os campos Título e Autor para buscar uma capa.
                            </div>
                        </div>
                        
                        <!-- Preview da capa selecionada -->
                        <div id="capa-preview" class="capa-preview" style="display: none;">
                            <p class="form-text">Capa selecionada:</p>
                            <img id="capa-preview-img" src="" alt="Preview da capa" class="capa-preview-image">
                        </div>

                        <div id="resultados-capas" class="capas-results-grid" style="display: none;"></div>
                        <input type="hidden" id="capa_url" name="capa_url" value="<%= typeof capa_url !== 'undefined' ? capa_url : '' %>">
                    </div>

                    <!-- Sinopse -->
                    <div class="form-group">
                        <label for="sinopse" class="form-label">
                            <i class="fas fa-align-left"></i> Sinopse
                        </label>
                        <textarea class="form-textarea" id="sinopse" name="sinopse" 
                                  placeholder="Escreva uma breve sinopse do livro"><%= typeof sinopse !== 'undefined' ? sinopse : '' %></textarea>
                        <div class="form-text">Opcional, mas recomendado para melhor descrição</div>
                    </div>

                    <!-- Ano e Categoria -->
                    <div class="form-row">
                        <div class="form-group">
                            <label for="ano" class="form-label">
                                <i class="fas fa-calendar"></i> Ano de Publicação *
                            </label>
                            <input type="number" class="form-input" id="ano" name="ano" 
                                   value="<%= typeof ano !== 'undefined' ? ano : new Date().getFullYear() %>" 
                                   min="1450" max="<%= new Date().getFullYear() %>" 
                                   placeholder="2023" required>
                            <div class="form-text">Ano de publicação original</div>
                        </div>
                        <div class="form-group">
                            <label for="categoria" class="form-label">
                                <i class="fas fa-tag"></i> Categoria *
                            </label>
                            <input type="text" class="form-input" id="categoria" name="categoria" 
                                value="<%= typeof categoria !== 'undefined' ? categoria : '' %>" 
                                placeholder="Ex: Romance, Ficção Científica, Biografia..." 
                                required>
                            <div class="form-text">Digite a categoria do livro</div>
                        </div>
                    </div>

                    <!-- Disponibilidade -->
                    <div class="form-group">
                        <div class="form-switch">
                            <label class="switch">
                                <%
                                let isDisponivel = true;
                                if (typeof disponivel !== 'undefined') {
                                    isDisponivel = disponivel;
                                }
                                %>
                                <input type="checkbox" id="disponivel" name="disponivel" value="true" 
                                       <%= isDisponivel ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                            <label for="disponivel" class="form-label">
                                <i class="fas fa-check-circle"></i> Disponível para empréstimo
                            </label>
                        </div>
                        <div class="form-text">Desmarque se o livro não estiver disponível para empréstimo</div>
                    </div>

                    <!-- Botões -->
                    <div class="form-actions">
                        <a href="/catalogo/acervo" class="btn btn-outline">
                            <i class="fas fa-arrow-left"></i> Voltar ao Acervo
                        </a>
                        <button type="submit" class="btn btn-primary" id="btnAdicionarLivro">
                            <i class="fas fa-plus-circle"></i> Adicionar Livro
                        </button>
                    </div>
                </form>
            </div>

            <!-- Info Card -->
            <div class="info-card card">
                <h3 class="info-title">
                    <i class="fas fa-info-circle"></i> Informações
                </h3>
                <ul class="info-list">
                    <li class="info-item">
                        <i class="fas fa-check-circle info-icon"></i>
                        <span>Livros adicionados ficam no <strong>acervo</strong> inicialmente</span>
                    </li>
                    <li class="info-item">
                        <i class="fas fa-check-circle info-icon"></i>
                        <span>Você pode publicá-los depois para aparecerem no catálogo</span>
                    </li>
                    <li class="info-item">
                        <i class="fas fa-check-circle info-icon"></i>
                        <span>Campos marcados com * são obrigatórios</span>
                    </li>
                </ul>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-brand">Biblioteca Ativa</div>
            <div class="footer-copyright">&copy; 2025 - Todos os direitos reservados</div>
        </div>
    </footer>
    <script src="/js/adicionarLivro.js"></script>
</body>
</html>