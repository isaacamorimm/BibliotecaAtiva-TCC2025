<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Biblioteca Ativa - Home</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {

            --primary: #9e4a3c;
            --primary-light: #c98c67;
            --primary-dark: #7a372b;
            --accent: #ff7e5f;
            --background: #faf7f4;
            --surface: #ffffff;
            --text: #333333;
            --text-light: #777777;
            --error: #e53935;
            --success: #43a047;
            --warning: #ff9800;
            --info: #2196f3;
            --border: #e0e0e0;
            --shadow-sm: 0 2px 8px rgba(0,0,0,0.05);
            --shadow-md: 0 4px 20px rgba(0,0,0,0.1);
            --radius: 12px;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: var(--background);
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            color: var(--text);
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Navbar */
        .navbar {
            background: var(--primary-dark);
            color: white;
            padding: 1rem 0;
            box-shadow: var(--shadow-md);
        }

        .navbar-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar-brand {
            font-size: 1.5rem;
            font-weight: 700;
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .navbar-nav {
            display: flex;
            list-style: none;
            gap: 20px;
            align-items: center;
        }

        .nav-link {
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 5px;
            padding: 8px 12px;
            border-radius: var(--radius);
            transition: var(--transition);
        }

        .nav-link:hover, .nav-link.active {
            background: rgba(255, 255, 255, 0.1);
        }

        .nav-dropdown {
            position: relative;
        }

        .nav-dropdown-toggle {
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .dropdown-menu {
            position: absolute;
            right: 0;
            top: 100%;
            background: white;
            border-radius: var(--radius);
            box-shadow: var(--shadow-md);
            min-width: 200px;
            padding: 10px 0;
            z-index: 1000;
            display: none;
        }

        .nav-dropdown:hover .dropdown-menu {
            display: block;
        }

        .dropdown-item {
            display: block;
            padding: 10px 20px;
            color: var(--text);
            text-decoration: none;
            transition: var(--transition);
        }

        .dropdown-item:hover {
            background: var(--background);
        }

        .dropdown-divider {
            height: 1px;
            background: var(--border);
            margin: 8px 0;
        }

        /* Hero Section */
        .hero-section {
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            color: white;
            padding: 3rem 0;
            margin-bottom: 2rem;
            text-align: center;
        }

        .hero-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
        }

        /* Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            width: 100%;
        }

        /* Grid System */
        .row {
            display: flex;
            flex-wrap: wrap;
            margin: 0 -15px;
        }

        .col {
            flex: 1;
            padding: 0 15px;
        }

        .col-12 {
            flex: 0 0 100%;
            max-width: 100%;
            padding: 0 15px;
        }

        @media (min-width: 768px) {
            .col-md-4 {
                flex: 0 0 33.333333%;
                max-width: 33.333333%;
            }
            .col-md-6 {
                flex: 0 0 50%;
                max-width: 50%;
            }
        }

        @media (min-width: 992px) {
            .col-lg-4 {
                flex: 0 0 33.333333%;
                max-width: 33.333333%;
            }
            .col-lg-6 {
                flex: 0 0 50%;
                max-width: 50%;
            }
        }

        /* Cards */
        .card {
            background: var(--surface);
            border-radius: var(--radius);
            box-shadow: var(--shadow-sm);
            margin-bottom: 20px;
            overflow: hidden;
            transition: var(--transition);
        }

        .card:hover {
            box-shadow: var(--shadow-md);
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .card-text {
            margin-bottom: 1rem;
        }

        /* Stats Cards */
        .stats-card {
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            color: white;
            text-align: center;
            height: 100%;
        }

        .welcome-card {
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            color: var(--text);
            text-align: center;
        }

        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        /* Buttons */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 12px 24px;
            background: var(--primary);
            color: white;
            text-decoration: none;
            border-radius: var(--radius);
            border: none;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            font-size: 1rem;
        }

        .btn:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(158, 74, 60, 0.25);
        }

        .btn-lg {
            padding: 16px 32px;
            font-size: 1.1rem;
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--primary);
            color: var(--primary);
        }

        .btn-outline:hover {
            background: var(--primary);
            color: white;
        }

        .btn-group {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 1.5rem;
        }

        /* Alerts */
        .alert {
            padding: 1rem 1.5rem;
            border-radius: var(--radius);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .alert-success {
            background: #e8f5e9;
            color: var(--success);
            border: 1px solid #c8e6c9;
        }

        .alert-error {
            background: #ffebee;
            color: var(--error);
            border: 1px solid #ffcdd2;
        }

        .alert-info {
            background: #e3f2fd;
            color: var(--info);
            border: 1px solid #bbdefb;
        }

        .alert-close {
            margin-left: auto;
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1.2rem;
        }

        /* Badges */
        .badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .badge-success {
            background: var(--success);
            color: white;
        }

        .badge-error {
            background: var(--error);
            color: white;
        }

        .badge-secondary {
            background: var(--text-light);
            color: white;
        }

        /* Book Card */
        .book-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 100%;
        }

        .book-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }

        .book-details p {
            margin-bottom: 0.5rem;
        }

        /* Footer */
        .footer {
            background: var(--primary-dark);
            color: white;
            padding: 2rem 0;
            margin-top: auto;
        }

        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .footer-col {
            flex: 1;
            min-width: 250px;
            margin-bottom: 1.5rem;
        }

        .footer h5 {
            font-size: 1.2rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .footer p {
            margin-bottom: 0.5rem;
        }

        .text-center {
            text-align: center;
        }

        .text-end {
            text-align: right;
        }

        /* Responsividade */
        @media (max-width: 768px) {
            .navbar-nav {
                flex-direction: column;
                gap: 10px;
                width: 100%;
            }
            
            .navbar-container {
                flex-direction: column;
                gap: 15px;
            }
            
            .btn-group {
                flex-direction: column;
            }
            
            .footer-container {
                flex-direction: column;
            }
            
            .text-end {
                text-align: left;
            }
            
            .hero-title {
                font-size: 2rem;
            }
        }

        .mb-3 { margin-bottom: 1rem; }
        .mb-4 { margin-bottom: 1.5rem; }
        .mb-5 { margin-bottom: 3rem; }
        .mt-3 { margin-top: 1rem; }
        .mt-4 { margin-top: 1.5rem; }
        .mt-5 { margin-top: 3rem; }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <div class="navbar-container">
            <a class="navbar-brand" href="/home">
                <i class="fas fa-book-open"></i> Biblioteca Ativa
            </a>
            <ul class="navbar-nav">
                <li>
                    <a class="nav-link active" href="/home">
                        <i class="fas fa-home"></i> Home
                    </a>
                </li>
                <li>
                    <a class="nav-link" href="/livros">
                        <i class="fas fa-book"></i> Catálogo
                    </a>
                </li>
                <% if (user && user.role === 'admin') { %>
                <li>
                    <a class="nav-link" href="/livros/acervo">
                        <i class="fas fa-archive"></i> Acervo
                    </a>
                </li>
                <li>
                    <a class="nav-link" href="/livros/adicionar">
                        <i class="fas fa-plus-circle"></i> Adicionar Livro
                    </a>
                </li>
                <% } %>
                <li class="nav-dropdown">
                    <div class="nav-link nav-dropdown-toggle">
                        <i class="fas fa-user-circle"></i> <%= user.nome %> <i class="fas fa-chevron-down"></i>
                    </div>
                    <div class="dropdown-menu">
                        <span class="dropdown-item">Função: <%= user.role %></span>
                        <span class="dropdown-item">Email: <%= user.email %></span>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout">
                            <i class="fas fa-sign-out-alt"></i> Sair
                        </a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="hero-section">
        <div class="container">
            <h1 class="hero-title">
                <i class="fas fa-book"></i> Biblioteca Ativa
            </h1>
            <p class="hero-subtitle">Sistema de gerenciamento bibliográfico completo</p>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container mb-5">
        <!-- Welcome Message -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="card welcome-card">
                    <div class="card-body">
                        <h2 class="card-title">
                            <i class="fas fa-smile"></i> Bem-vindo, <%= user.nome %>!
                        </h2>
                        <p class="card-text">
                            Você está logado como: <strong><%= user.role %></strong>
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Alertas -->
        <% if (success) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i> 
                <strong>Sucesso!</strong> <%= success %>
                <button class="alert-close">&times;</button>
            </div>
        <% } %>

        <% if (error) { %>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-triangle"></i> 
                <strong>Erro:</strong> <%= error %>
                <button class="alert-close">&times;</button>
            </div>
        <% } %>

        <!-- Role Specific Content -->
        <% if (user.role === 'admin') { %>
        <div class="row mb-4">
            <div class="col-md-4 mb-3">
                <div class="card stats-card">
                    <div class="card-body">
                        <i class="fas fa-book feature-icon"></i>
                        <h3>Gestão Completa</h3>
                        <p>Administre todos os aspectos da biblioteca</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
                <div class="card stats-card">
                    <div class="card-body">
                        <i class="fas fa-users feature-icon"></i>
                        <h3>Controle Total</h3>
                        <p>Gerencie livros, usuários e empréstimos</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
                <div class="card stats-card">
                    <div class="card-body">
                        <i class="fas fa-chart-line feature-icon"></i>
                        <h3>Relatórios</h3>
                        <p>Acesse estatísticas e relatórios detalhados</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mb-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-body text-center">
                        <h4 class="card-title">
                            <i class="fas fa-tools"></i> Painel Administrativo
                        </h4>
                        <div class="btn-group">
                            <a class="btn btn-lg" href="/livros" role="button">
                                <i class="fas fa-book"></i> Ver Todos os Livros
                            </a>
                            <a class="btn btn-lg" href="/livros/adicionar" role="button">
                                <i class="fas fa-plus-circle"></i> Adicionar Livro
                            </a>
                            <a class="btn btn-lg" href="/livros/acervo" role="button">
                                <i class="fas fa-archive"></i> Gerenciar Acervo
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <% } else { %>
        <div class="row mb-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-body text-center">
                        <h4 class="card-title">
                            <i class="fas fa-search"></i> Explorar Biblioteca
                        </h4>
                        <p class="card-text">Descubra nosso acervo de livros disponíveis para empréstimo</p>
                        <a class="btn btn-lg" href="/livros" role="button">
                            <i class="fas fa-book"></i> Explorar Catálogo
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Livros em Destaque -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">
                            <i class="fas fa-star"></i> Livros em Destaque
                        </h4>
                        
                        <div class="row">
                            <% if (livros && livros.length === 0) { %>
                                <div class="col-12">
                                    <div class="alert alert-info text-center">
                                        <i class="fas fa-info-circle" style="font-size: 3rem; margin-bottom: 1rem;"></i>
                                        <h5>Nenhum livro cadastrado no sistema</h5>
                                        <% if (user && user.role === 'admin') { %>
                                            <a href="/livros/adicionar" class="btn mt-3">
                                                <i class="fas fa-plus-circle"></i> Adicionar Primeiro Livro
                                            </a>
                                        <% } %>
                                    </div>
                                </div>
                            <% } else { %>
                                <% livros.slice(0, 6).forEach(livro => { %>
                                    <div class="col-lg-4 col-md-6 mb-4">
                                        <div class="card book-card">
                                            <div class="card-body">
                                                <h5 class="card-title" style="color: var(--primary);"><%= livro.titulo %></h5>
                                                <h6 class="mb-3" style="color: var(--text-light);">
                                                    <i class="fas fa-user"></i> <%= livro.autor %>
                                                </h6>
                                                <div class="book-details">
                                                    <p>
                                                        <strong><i class="fas fa-calendar"></i> Ano:</strong> <%= livro.ano %>
                                                    </p>
                                                    <p>
                                                        <strong><i class="fas fa-tag"></i> Categoria:</strong> 
                                                        <span class="badge badge-secondary"><%= livro.categoria %></span>
                                                    </p>
                                                    <p>
                                                        <strong>Disponibilidade:</strong> 
                                                        <span class="badge <%= livro.disponivel ? 'badge-success' : 'badge-error' %>">
                                                            <i class="fas <%= livro.disponivel ? 'fa-check-circle' : 'fa-times-circle' %>"></i>
                                                            <%= livro.disponivel ? 'Disponível' : 'Indisponível' %>
                                                        </span>
                                                    </p>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } %>
                        </div>

                        <% if (livros && livros.length > 6) { %>
                            <div class="text-center mt-4">
                                <a class="btn btn-outline" href="/livros">
                                    <i class="fas fa-arrow-right"></i> Ver Todos os Livros
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-col">
                <h5><i class="fas fa-book"></i> Biblioteca Ativa</h5>
                <p>Sistema de gerenciamento de acervo bibliográfico</p>
            </div>
            <div class="footer-col text-end">
                <p>&copy; 2025 Biblioteca Ativa. Todos os direitos reservados.</p>
                <p>Sistema desenvolvido para ETEC</p>
            </div>
        </div>
    </footer>

    <script>
        // Fechar alertas
        document.querySelectorAll('.alert-close').forEach(button => {
            button.addEventListener('click', function() {
                this.parentElement.style.display = 'none';
            });
        });

        // Menu mobile (simplificado)
        document.addEventListener('DOMContentLoaded', function() {
            const dropdowns = document.querySelectorAll('.nav-dropdown');
            
            dropdowns.forEach(dropdown => {
                dropdown.addEventListener('click', function(e) {
                    if (window.innerWidth < 768) {
                        const menu = this.querySelector('.dropdown-menu');
                        menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
                        e.stopPropagation();
                    }
                });
            });
            
            // Fechar dropdowns ao clicar fora
            document.addEventListener('click', function() {
                document.querySelectorAll('.dropdown-menu').forEach(menu => {
                    menu.style.display = 'none';
                });
            });
        });
    </script>
</body>
</html>
